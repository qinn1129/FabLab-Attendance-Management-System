{
  "name": "fablab-mongo-service",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "MongoDB-backed replacement for AppsScriptMirror.gs. Mirrors the same doGet/doPost contract (sheet name + action) so the React frontend needed minimal changes.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "node migrate.js",
    "seed-admin": "node seedAdmin.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parse": "^5.5.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^7.5.0",
    "mongoose": "^8.5.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
